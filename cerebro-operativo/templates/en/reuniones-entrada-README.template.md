Drop a transcript here by hand when Fathom did not record the meeting. Put the date first (`YYYY-MM-DD-<title>.md`). The next run of the routine picks it up, processes it and keeps it whole inside the meeting file in `meetings/` (nothing is lost: the original text stays there); if you are in a hurry, ask in the chat "process the transcript I left in the inbox".
