name: check

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  gates:
    name: LF, dashes, secrets, personal data, manifests, stamped skills
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run the gates
        run: bash scripts/check.sh
